// listen for the browser's install event.
// save it until the user clicks Install.

// expose:
// canInstall
// installApp()

// then SettingsCard won't need to know any of the browser details—it will just use the hook.